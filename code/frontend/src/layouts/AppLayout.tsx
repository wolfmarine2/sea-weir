/**
 * 主布局:顶栏 + 侧边导航 + 内容区。
 *
 * 侧边导航项的显隐由 statusStore 的 features 驱动(服务端下发),
 * 可见性判断与角色守卫是两件事:features 控制「有没有这个模块」,
 * role 控制「这个用户能不能进」。
 */
export default function AppLayout() {
  // TODO(TDD): antd Layout;顶栏含主题切换、语言切换、用户菜单
  return null;
}
