/**
 * 通用组件。
 *
 * TODO(TDD):
 * - SecureVerifyModal —— 敏感操作二次验证(密码/2FA/Passkey 换凭证,对接 POST /api/verify)
 * - QuotaDisplay      —— 额度 ↔ 金额换算展示(QUOTA_PER_UNIT = 500000)
 * - StatusBadge       —— 状态码 → 徽标
 * - ConfirmButton     —— 危险操作二次确认
 * - CopyableSecret    —— 密钥展示与复制(默认脱敏)
 */
export {};
