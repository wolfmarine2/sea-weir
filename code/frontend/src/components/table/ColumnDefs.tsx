/** 列定义工厂。各域提供自己的列描述,此处提供通用渲染器(时间、额度、状态徽标等)。 */

// TODO(TDD): renderQuota(quota → $ 金额,按 QUOTA_PER_UNIT=500000 换算)
// TODO(TDD): renderTimestamp(unix 秒 → 本地时间)
// TODO(TDD): renderStatus(状态码 → 徽标)
export {};
